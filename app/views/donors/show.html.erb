<h1 id='donor-title'><%= @donor.first_name %> <%= @donor.last_name %></h1>

<%= link_to "Edit", edit_donor_path(@donor), remote: true %>

<%= link_to "Delete", donor_path(@donor), data: {
          confirm: "Are you sure that you want to delete this account?",
          method: 'delete'
} %>

<div id="edit-donor"></div>
